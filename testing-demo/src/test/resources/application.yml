spring:
  application:
    name: testing
  datasource:
    username: sa
    password: sa
    url: jdbc:h2:mem:testdb
  jpa:
    showSql: true
    hibernate:
      ddlAuto: create-drop
server:
  port: 10000